#include "header.h"

int main(void)
{
    foodcart_data fd_data;
    ifstream in_file;
    in_file.open("class_var_list.txt");
    char delimiter = ":";
    int one_index = 0, two_index = 0; three_index = 0;

    if(in_file)
    {

    return 0;
}
