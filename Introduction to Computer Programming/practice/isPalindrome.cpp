#include <iostream>

using namespace std;

bool palinDrome( int foward[], int backward[]);

int main()
{
   int forward[10], int backward[10];

   cin.get(forward, 10, '\n');
    
   return 0;
}

bool palinDrome( int forward[], int backward[])
{
    
